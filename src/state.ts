import { State } from "./api"

export const state: State = {
  runs: {},
  logs: [],
  paneDisplay: "right",
  valueDisplay: "state",
  paneShown: false,
  selectedAction: null,
  collapseRepeatingActions: true,
  ObjectView: {}
}
